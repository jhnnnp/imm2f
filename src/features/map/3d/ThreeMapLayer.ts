/** V1.5 extension point for Three.js memory objects (CustomLayerInterface). */
export type MemoryObjectKind = "memory_pin" | "anniversary_pin" | "route_glow";

export const THREE_LAYER_ID = "only-us-memory-objects";
